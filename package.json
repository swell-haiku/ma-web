{
  "name": "ma-web",
  "version": "1.0.0",
  "description": "MutualDebate - Global debate platform",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev -w frontend\" \"npm run dev -w backend\"",
    "build": "npm run build -w frontend && npm run build -w backend",
    "start": "concurrently \"npm run start -w frontend\" \"npm start -w backend\"",
    "lint": "npm run lint -w frontend && npm run lint -w backend"
  },
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
